import React from 'react';
import { RouteComponentProps } from '@reach/router';
import Layout from './Layout';

const Services = (props: RouteComponentProps) => {
  return (
    <Layout>
      <h1>Services</h1>
    </Layout>
  );
};

export default Services;
