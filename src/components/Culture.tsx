import React from 'react';
import { RouteComponentProps } from '@reach/router';
import Layout from './Layout';

const Culture = (props: RouteComponentProps) => {
  return (
    <Layout>
      <h1>Culture</h1>
    </Layout>
  );
};

export default Culture;
