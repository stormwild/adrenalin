import React from 'react';
import { RouteComponentProps } from '@reach/router';
import Layout from './Layout';

const Careers = (props: RouteComponentProps) => {
  return (
    <Layout>
      <h1>Careers</h1>
    </Layout>
  );
};

export default Careers;
