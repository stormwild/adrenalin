import React from 'react';
import { RouteComponentProps } from '@reach/router';
import Layout from './Layout';

const Sitemap = (props: RouteComponentProps) => {
  return (
    <Layout>
      <h1>Sitemap</h1>
    </Layout>
  );
};

export default Sitemap;
