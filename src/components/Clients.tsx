import React from 'react';
import { RouteComponentProps } from '@reach/router';
import Layout from './Layout';

const Clients = (props: RouteComponentProps) => {
  return (
    <Layout>
      <h1>Clients</h1>
    </Layout>
  );
};

export default Clients;
